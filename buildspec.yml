version: 1.2

phases:
  install:
	commands:
		- apt install maven 
  build:
    commands:
      - echo Build started on `date`
      - mvn test
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
artifacts:
  files:
    - target/finalcalci-1.2-SNAPSHOT.war
